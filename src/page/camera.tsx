import React from "react"
import {
    Text,
    StyleSheet
} from "react-native"

const Camera = () => {
    return (
        <Text> this Camera</Text>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingVertical: 30,
    },
})

export default Camera