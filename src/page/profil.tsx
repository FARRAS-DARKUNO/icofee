import React from "react"
import {
    Text,
    StyleSheet
} from "react-native"

const Profil = () => {
    return (
        <Text> this Profil</Text>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingVertical: 30,
    },
})

export default Profil