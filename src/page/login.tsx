import React from "react"
import {
    Text,
    StyleSheet
} from "react-native"

const Login = () => {
    return (
        <Text> this Login</Text>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingVertical: 30,
    },
})

export default Login