import React from "react"
import {
    Text,
    StyleSheet
} from "react-native"

const NotificationDetail = () => {
    return (
        <Text> this NotificationDetail</Text>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingVertical: 30,
    },
})

export default NotificationDetail