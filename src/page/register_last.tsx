import React from "react"
import {
    Text,
    StyleSheet
} from "react-native"

const RegisterLast = () => {
    return (
        <Text> this RegisterLast</Text>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingVertical: 30,
    },
})

export default RegisterLast