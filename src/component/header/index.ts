import UserHeaders from "./user_header"
import BackHeader from "./back_header"
import HeaderTittle from "./header_tiitle"
import BackRemoveHeader from "./back_remove_header"

const Header = {
    UserHeaders,
    BackHeader,
    HeaderTittle,
    BackRemoveHeader,
}

export default Header